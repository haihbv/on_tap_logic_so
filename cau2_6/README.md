Viết mô tả `Verilog` và viết `testbench` cho 2 mạch tổ hợp như sau:
a. bộ cộng 4 bit không dấu có nhớ.

b. bộ giải mã địa chỉ 3:8 với các đầu vào/ra hoạt động ở mức tích cực thấp và có một lối vào cho phép hoạt động ở mức tích cực thấp.

