# NGÂN HÀNG THIẾT KẾ LOGIC SỐ

> Ngày tạo: 04/06/2026 
> Người đăng: Hải Bùi Văn D23

## PHẦN I. CÂU HỎI LOẠI 1 ĐIỂM

Câu 1.1 Kiến trúc cơ bản hệ thống số gồm những phần nào? Trình bày ngắn gọn đặc điểm của mỗi phần.

Câu 1.2 Trình bày ngắn gọn nội dung các mức phát triển của hệ thống số?

Câu 1.3 Vẽ sơ đồ hệ thống các công nghệ số? Đặc điểm của mỗi công nghệ đó?

Câu 1.4 Tiêu chí so sánh các loại cấu kiện chuẩn? với các tiêu chí đó thì FPGA được đánh giá ntn?

Câu 1.5 Trình bày ngắn gọn quy trình thiết kế vi điện tử?

Câu 1.6 Thiết kế số sử dụng HDL có ưu điểm gì so với thiết kế truyền thống?

Câu 1.7 Trình bày ngắn gọn quy trình thiết kế vi điện tử sử dụng HDL?

Câu 1.8 Kể tên và nêu tóm tắt đặc điểm của 4 phần mềm phổ biến trong thiết kế logic số hiện nay?

Câu 1.9 Ưu điểm của kiến trúc PLA so với PAL? Lấy ví dụ minh họa.

Câu 1.10: Mô tả kiến trúc PLA? Lấy ví dụ minh họa.

Câu 1.11: Mô tả kiến trúc PAL? Lấy ví dụ minh họa?

Câu 1.12: Mô tả ngắn gọn cấu trúc CPLD?

Câu 1.14: Ưu điểm chính của CPLD so với FPGA là gì? Ứng dụng CPLD?

Câu 1.15: Ưu điểm chính của FPGA so với CPLD là gì? Ứng dụng FPGA?

Câu 1.16: Trình bày ngắn gọn phương pháp thiết lập cấu hình cho CPLD/ FPGA dùng sơ đồ mô tả?

Câu 1.17: Trình bày ngắn gọn phương pháp thiết lập cấu hình cho CPLD/ FPGA dùng ngôn ngữ phần cứng?

Câu 1.18: Nêu những điểm cần chú ý khi lựa chọn vi mạch CPLD/ FPGA để thiết lập hệ thống số?

Câu 1.19: Trình bày các kiểu lập trình ngay trên hệ thống khi cấu hình cho CPLD/FPGA?

Câu 1.20: Trình bày ngắn gọn các đặc điểm của giải pháp cấu hình hệ thống ACE?

Câu 1.21: Lấy ví dụ về sự khác nhau trong gói phần mềm của nhà cung cấp Xilinx?

Câu 1.22: Vẽ lưu đồ các bước thiết kế CPLD?

Câu 1.23: Vẽ lưu đồ các bước thiết kế FPGA?

Câu 1.24: Sự khác nhau trong các bước thiết kế FPGA và CPLD là gì? Lý do dẫn đến sự khác nhau đó?

Câu 1.25: Trình bày đặc điểm và ý nghĩa của Family, Device, Package, Speed khi lựa chọn linh kiện trong thiết kế CPLD/ FPGA sử dụng phần mềm ISE?

Câu 1.26: Trình bày ngắn gọn đặc điểm và chức năng của các file nguồn Module, Library, Pakage, TestBench trong ngôn ngữ mô tả phần cứng VHDL?

Câu 1.27: Ưu điểm và ứng dụng của ngôn ngữ VHDL?

Câu 1.28: Trình bày ngắn gọn cấu trúc cơ bản của VHDL?

Câu 1.29: Kể tên các đối tượng trong VHDL? Nêu ngắn gọn chức năng và viết cú pháp tương ứng cho các đối tượng?

Câu 1.30: Sự khác nhau về việc sử dụng kiểu dữ liệu std_logic và std_ulogic trong quá trình chạy mô phỏng hay tổng hợp mã từ VHDL?

Câu 1.31: Kể tên các kiểu dữ liệu chính trong dạng dữ liệu vô hướng? Xác định biểu diễn dưới dạng cơ số chính xác của số 1003 trong kiểu số nguyên và số thực?

Câu 1.32: Phân biệt sự khác nhau cơ bản giữa Signal (tín hiệu) và Variable (biến)?

Câu 1.33 Mô tả cấu trúc chọn kênh 4 đầu vào, 1 đầu ra Z và tín hiệu chọn kênh SEL 2 bit bằng cấu trúc lệnh song song when/else và cấu trúc lệnh tuần tự.

**Câu 1.34**: Viết mô tả VHDL cho mạch biến đổi mã Gray – Binary 4 bit dùng cấu trúc lệnh song song?

**Câu 1.35**: Viết mô tả VHDL cho mạch biến đổi mã Gray- Binary 4 bit dùng cấu trúc lệnh tuần tự.

**Câu 1.36**: Viết mô tả VHDL cho mạch biến đổi mã Binary – Gray 4 bit sử dụng cấu trúc lệnh song song.

**Câu 1.37**: Viết mô tả VHDL cho mạch biến đổi mã Binary – Gray 4 bit sử dụng cấu trúc lệnh tuần tự.

**Câu 1.38**: Viết mô tả VHDL cho mạch cộng 4 bit KHÔNG dấu bằng cấu trúc lệnh song song.

**Câu 1.39**: Viết mô tả VHDL cho mạch cộng 4 bit CÓ dấu bằng cấu trúc lệnh song song.

**Câu 1.40**: Viết mô tả VHDL cho bộ đếm nhị phân đơn giản.

**Câu 1.41**: Viết mô tả VHDL cho trigơ D có các đầu vào lập và xóa mức tích cực cao?

```verilog
module system_top(
	input clk,
	input rst_n,
	input d,
	input pre,
	input clr,
	output reg q,
	output q_n
);
	
	assign q_n = ~q;
	
	always @(posedge clk or negedge rst_n or posedge pre or posedge clr) begin
		if (!rst_n) q <= 0;
		else if (pre) q <= 1;
		else if (clr) q <= 0;
		else q <= d;
	end

endmodule
```

**Câu 1.42**: Viết mô tả VHDL cho trigơ D có các đầu vào lập và xóa mức tích cực thấp?

```verilog
module system_top(
	input clk,
	input rst_n,
	input d,
	input pre_n,
	input clr_n,
	output reg q,
	output q_n
);
	assign q_n = ~q;
	
	always @(posedge clk or negedge rst_n or negedge pre_n or negedge clr_n) begin
		if (!rst_n) q <= 0;
		else if (!clr_n) q <= 0;
		else if (!pre_n) q <= 1;
		else q <= d;
	end

endmodule
```

**Câu 1.43**: Viết mô tả VHDL cho trigơ JK có các đầu vào lập và xóa mức tích cực cao?

```verilog
module system_top(
	input clk, 
	input rst_n,
	input j,
	input k,
	input pre,
	input clr,
	output reg q,
	output q_n
);

	assign q_n = ~q;
	
	always @(posedge clk or negedge rst_n or posedge pre or posedge clr) begin
		if (!rst_n) q <= 0;
		else if (pre) q <= 1;
		else if (clr) q <= 0;
		else q <= (~k & q) | (j & ~q);
	end
endmodule
```

**Câu 1.44**: Viết mô tả VHDL cho trigơ JK có các đầu vào lập và xóa mức tích cực thấp?

```verilog
module system_top(
	input clk, 
	input rst_n,
	input j,
	input k,
	input pre_n,
	input clr_n,
	output reg q,
	output q_n
);

	assign q_n = ~q;
	
	always @(posedge clk or negedge rst_n or negedge pre_n or negedge clr_n) begin
		if (!rst_n) q <= 0;
		else if (!pre_n) q <= 1;
		else if (!clr_n) q <= 0;
		else q <= (~k & q) | (j & ~q);
	end
endmodule
```
**Câu 1.45**: Viết mô tả VHDL cho trigơ RS có các đầu vào lập và xóa mức tích cực cao?

```verilog
module system_top(
	input clk,
	input rst_n,
	input pre,
	input clr,
	input r,
	input s,
	output reg q,
	output q_n
);

	assign q_n = ~q;
	
	always @(posedge clk or negedge rst_n or posedge pre or posedge clr) begin
		if (!rst_n) q <= 0;
		else if (pre) q <= 1;
		else if (clr) q <= 0;
		else q = (s | (~r & q));
	end
	
endmodule
```

**Câu 1.46**: Viết mô tả VHDL cho trigơ RS có các đầu vào lập và xóa mức tích cực thấp?

```verilog
module system_top(
	input clk,
	input rst_n,
	input pre_n,
	input clr_n,
	input r,
	input s,
	output reg q,
	output wire q_n
);

	assign q_n = ~q;
	
	always @(posedge clk or negedge rst_n or negedge pre_n or negedge clr_n) begin
		if (!rst_n) q <= 0;
		else if (!pre_n) q <= 1;
		else if (!clr_n) q <= 0;
		else q <= (s | (~r&q));
	end
endmodule
```

**Câu 1.47**: Viết mô tả VHDL cho bộ đếm thập phân tiến 2 digit có Reset không đồng bộ?

```verilog
module led7Seg(
	input [3:0] num,
	output reg [7:0] pin
);
	always @(*) begin
		case (num)
			4'd0: pin = 8'hC0;
            4'd1: pin = 8'hF9;
            4'd2: pin = 8'hA4;
            4'd3: pin = 8'hB0;
            4'd4: pin = 8'h99;
            4'd5: pin = 8'h92;
            4'd6: pin = 8'h82;
            4'd7: pin = 8'hF8;
            4'd8: pin = 8'h80;
            4'd9: pin = 8'h90;
			default: pin = 8'hff;
		endcase
	end
endmodule

module system_top(
	input clk,
	input rst_n,
	output [7:0] chuc_out,
	output [7:0] donvi_out
);
	reg [3:0] chuc, donvi;
	
	led7Seg led_chuc(.num(chuc), .pin(chuc_out));
	led7Seg led_donvi(.num(donvi), .pin(donvi_out));
	
	always @(posedge clk or negedge rst_n) begin
		if (!rst_n) begin
			chuc <= 4'd0;
			donvi <= 4'd0;
		end
		else begin
			if (donvi == 4'd9) begin
				donvi <= 4'd0;
				if (chuc == 4'd9) chuc <= 4'd0;
				else chuc <= chuc + 1;
			end
			else donvi <= donvi + 1;
		end
	end

endmodule
```

**Câu 1.48**: Viết mô tả VHDL cho bộ đếm thập phân tiến 2 digit có Reset đồng bộ?

```verilog
module led7Seg(
	input [3:0] num,
	output reg [7:0] pin
);
	always @(*) begin
		case (num)
			4'd0: pin <= 8'hC0;
			4'd1: pin <= 8'hF9;
			4'd2: pin <= 8'hA4;
			4'd3: pin <= 8'hB0;
			4'd4: pin <= 8'h99;
			4'd5: pin <= 8'h92;
			4'd6: pin <= 8'h82;
			4'd7: pin <= 8'h8F;
			4'd8: pin <= 8'h80;
			4'd9: pin <= 8'h90;
			default: pin <= 8'h00;
		endcase
	end
endmodule

module twoDigitDecimalCounter(
	input clk,
	input rst_n,
	output [7:0] chuc_out,
	output [7:0] donvi_out
);
	
	reg [3:0] chuc, donvi;
	
	led7Seg led_chuc(
		.num(chuc),
		.pin(chuc_out)
	);
	
	led7Seg led_donvi(
		.num(donvi),
		.pin(donvi_out)
	);
	
	always @(posedge clk) begin
		if(!rst_n) begin
			chuc <= 4'd0;
			donvi <= 4'd0;
		end
		else begin
			if(donvi == 4'd9) begin
				donvi <= 4'd0;
				if(chuc == 4'd9) chuc <= 4'd0;
				else chuc <= chuc + 1;
			end
			else donvi <= donvi + 1;
		end
	end
endmodule
```

## PHẦN II. CÂU HỎI LOẠI 2 ĐIỂM

Câu 2.1 Viết mô tả VHDL và viết testbench cho Bộ đếm thập phân 2-digit với các đặc điểm: Đếm lùi mode 60, Clock sườn dương, chức năng Reset đồng bộ, chức năng Load nạp bằng số) đồng bộ, chức năng cho phép CE mức tích cực cao. (Hiển thị kết quả ra LED 7 đoạn)

Câu 2.2 Viết mô tả VHDL và viết testbench cho Bộ đếm thập phân 2-digit với các đặc điểm: Đếm lùi mode 90, Clock sườn âm, chức năng Reset không đồng bộ, chức năng Load nạp bằng số) đồng bộ, chức năng cho phép CE mức tích cực thấp. (Hiển thị kết quả ra LED 7 đoạn)

Câu 2.3 Viết mô tả VHDL và viết testbench cho Bộ đếm nhị phân 8-bit với các đặc điểm: Đếm tiến và đếm lùi, Clock sườn âm, chức năng Reset không đồng bộ, chức năng Load nạp bằng số) đồng bộ, chức năng cho phép CE mức tích cực thấp. (Hiển thị kết quả ra LED 7 đoạn)

Câu 2.4 Viết mô tả VHDL và viết testbench cho Bộ đếm nhị phân 8-bit với các đặc điểm: Đếm tiến và đếm lùi, Clock sườn dương, chức năng Reset đồng bộ, chức năng Load nạp bằng số) đồng bộ, chức năng cho phép CE mức tích cực cao. (Hiển thị kết quả ra LED 7 đoạn)

Câu 2.5 Viết mô tả VHDL và viết testbench cho 2 mạch tổ hợp sau:

Câu 2.6 Viết mô tả VHDL và viết testbench cho 2 mạch tổ hợp sau:

Câu 2.7 Viết mô tả VHDL và viết testbench cho 2 mạch tổ hợp sau:

Câu 2.8 Viết mô tả VHDL và viết testbench cho 2 mạch tổ hợp sau:

Câu 2.9: Viết mô tả cho mạch VHDL cho mạch giải mã từ nhị phân sang thập phân, sang gray và ngược lại?

Câu 2.10: Viết mô tả VHDL cho IC ghi dịch 8 bit hoạt động tại sườn dương clock, có tín hiệu chốt clock (CE) mức tích cực cao, tín hiệu lập không đồng bộ tích cực cao?

Câu 2.11 Viết mô tả VHDL cho mạch sau:

Câu 2.12 Viết mô tả VHDL cho mạch sau:

Câu 2.13 Viết mô tả VHDL cho:

Câu 2.14: Viết mô tả VHDL cho:

Câu 2.15: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:

Câu 2.16: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:

Câu 2.17: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:

Câu 2.18: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:

Câu 2.19: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:

Câu 2.20: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:

Câu 2.21: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:

Câu 2.22: Vẽ sơ đồ phần cứng tổng hợp được với đoạn code mô tả sau:


## PHẦN III. CÂU HỎI LOẠI 3 ĐIỂM

Câu 3.1: Vẽ mô hình (ghi chú rõ ràng trên sơ đồ khối, không cần giải thích chức năng các khối) máy bán hàng nước coca tự động đơn giản (nhận loại xu VNĐ: 500, 1000, 2000, giá 1 chai coca: 3000 VNĐ), vẽ mô hình máy trạng thái hữu hạn FSM và viết mô tả VHDL mô tả chức năng cho bộ điều khiển của máy bán hàng đó.

Câu 3.2: Vẽ mô hình (ghi chú rõ ràng trên sơ đồ khối, không cần giải thích chức năng các khối) của hệ thống đèn giao thông đơn giản tại ngã tư (Clk = 4MHz, chế độ đêm: chỉ có các đèn vàng cùng sáng, chế độ ban ngày thời gian chuyển các đèn như sau: Xanh 22s, Vàng 3s, Đỏ 25s), Vẽ đồ hình máy trạng thái hữu hạn FSM và viết mô tả VHDL mô tả chức năng cho bộ điều khiển của hệ thống đèn giao thông đó.

Câu 3.3: Vẽ mô hình (ghi chú rõ ràng trên sơ đồ khối, không cần giải thích chức năng các khối) máy bán hàng nước cam tự động đơn giản (nhận loại cent: 10, 20, 40, giá 1 chai coca: 50 cent), vẽ mô hình máy trạng thái hữu hạn FSM và viết mô tả VHDL mô tả chức năng cho bộ điều khiển của máy bán hàng đó.

Câu 3.4: Vẽ mô hình (ghi chú rõ ràng trên sơ đồ khối, không cần giải thích chức năng các khối) của hệ thống đèn giao thông đơn giản tại ngã tư (Clk = 20MHz, chế độ đêm: chỉ có các đèn vàng cùng sáng, chế độ ban ngày thời gian chuyển các đèn như sau: Xanh 40s, Vàng 5s, Đỏ 45s), Vẽ đồ hình máy trạng thái hữu hạn FSM và viết mô tả VHDL mô tả chức năng cho bộ điều khiển của hệ thống đèn giao thông đó.

Câu 3.5: Xây dựng mạch đếm thuận nghịch mode 10, đồng bộ với yêu cầu sau:

Câu 3.6: Xây dựng mạch đếm thuận nghịch mode 10, đồng bộ với yêu cầu sau:

Câu 3.7: Viết mô tả VHDL cho bộ đếm thập phân lên xuống 3 số có giá trị từ 123 đến 678, có tín hiệu nạp song song tích cực cao, tín hiệu Reset tích cực thấp?

Câu 3.8: Viết mô tả VHDL cho mạch ALU đơn giản.

Câu 3.9: Viết mô tả VHDL cho mạch nhân và chia đơn giản 8 bit dấu phẩy tĩnh.

Câu 3.10: Viết mô tả VHDL cho mạch cộng / trừ 8 bit có dấu và không dấu bằng cấu trúc lệnh song song.

Câu 3.11: Cho sơ đồ như hình vẽ. Giải thích ngắn gọn hoạt động của mạch? Viết mô tả VHDL cho mạch đó. Viết test bench để kiểm tra hoạt động của mạch.

Câu 3.12 Cho sơ đồ. Giải thích ngắn gọn hoạt động của mạch? Viết mô tả VHDL cho mạch sau:

Câu 3.13 Cho sơ đồ như hình vẽ. Giải thích ngắn gọn hoạt động của mạch? Viết mô tả VHDL (Entity và Architecture) cho mạch. Viết testbench để kiểm tra hoạt động của mạch.

Câu 3.14 Viết mô tả VHDL cho mạch có sơ đồ như sau:

Câu 3.15 Viết mô tả VHDL cho mạch có sơ đồ sau:
