Viết mô tả `Verilog` và viết `testbench` cho 2 mạch tổ hợp như sau:
a. bộ `DEMUX 1:8` bằng cấu trúc `case...is`.

Xem tại [phần a](phan_a/)

b. bộ `mã hóa ưu tiên 8:3` với các đầu vào/ra hoạt động ở mức tích cực thấp và có một lối vào cho phép hoạt động ở `mức tích cực thấp`.

Xem tại [phần b](phan_b/)